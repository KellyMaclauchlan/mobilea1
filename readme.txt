In the serverconnection.java file in the android project the url must be changed to mach the server IP address 
 
This project assumes that the database aone  with collections graph and pages 

In the controller class you can change the number of pages to be crawled and the web.xml must be set up to the correct IP address